#!/bin/bash
THRESHOLD=1000000  # 125MB in KB
while true; do
    FREE_MEM=$(free | grep Mem | awk '{print $4}')
    if [ "$FREE_MEM" -lt "$THRESHOLD" ]; then
        echo "Memory low: $(date)" | mail -s "Free memory space"   michaelndoh9@gmail.com 
    fi
    
